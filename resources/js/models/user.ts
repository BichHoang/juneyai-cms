export interface User {
    email: string
    firstName: string
    lastName: string
    avatarUrl: string
    phone: string
    createdAt: string
    updatedAt: string
    roles: string[]
    permissions: string[]
}
